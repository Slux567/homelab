apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: public-wildcard-cert
spec:
  secretName: public-wildcard-cert
  issuerRef:
    name: letsencrypt-prod-cloudflare
    kind: ClusterIssuer
  dnsNames:
  - "${SECRET_DOMAIN}"
  - "*.${SECRET_DOMAIN}"