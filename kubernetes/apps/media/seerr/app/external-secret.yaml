---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: seerr-api-key
spec:
  length: 32
  digits: 10
  symbols: 10
  symbolCharacters: "@!"
  noUpper: false
  allowRepeat: true
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: seerr
spec:
  refreshInterval: 1h
  refreshPolicy: CreatedOnce
  target:
    name: seerr
    template:
      engineVersion: v2
      data:
        seerr__AUTH__APIKEY: "{{ .password }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: seerr-api-key