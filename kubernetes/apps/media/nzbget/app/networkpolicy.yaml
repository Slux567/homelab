apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: nzbget-allow-egress-usenet
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: plex
  policyTypes:
  - Egress
  egress:
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
    ports:
    - protocol: TCP
      port: 563