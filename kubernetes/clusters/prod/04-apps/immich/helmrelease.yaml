apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
  namespace: immich
spec:
  interval: 5m
  chart:
    spec:
      chart: immich
      version: 0.10.0
      sourceRef:
        kind: HelmRepository
        name: immich-charts
        namespace: flux-system
      interval: 1m

  valuesFrom:
    - kind: ConfigMap
      name: immich-values
      valuesKey: values.yaml
    - kind: Secret
      name: immich-secrets
      valuesKey: values.yaml
