apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - deployment.yaml
  - service.yaml
  - storage.yaml
  - ingress.yaml
configMapGenerator:
  - name: server-config
    files: 
      - config/server.yml
generatorOptions:
  disableNameSuffixHash: true