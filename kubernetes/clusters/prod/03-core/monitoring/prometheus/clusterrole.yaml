apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: prometheus-metrics-resources
rules:
  - nonResourceURLs:
      - "/metrics"
      - "/metrics/resources"
    verbs:
      - get
