apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: truenas-transport
  namespace: truenas
spec:
  # This tells Traefik to accept the self-signed certificate from TrueNAS
  insecureSkipVerify: true
  serverName: "10.40.10.10"
