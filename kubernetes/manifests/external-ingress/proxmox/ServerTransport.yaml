apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: proxmox-transport
  namespace: external-ingress
spec:
  # This tells Traefik to accept the self-signed certificate from proxmox
  insecureSkipVerify: true
  serverName: "192.168.20.10"
