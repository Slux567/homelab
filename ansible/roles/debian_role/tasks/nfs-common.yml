- name: Install NFS Common
  apt:
    name: nfs-common
    state: present
    update_cache: yes

- name: Configure multipath
  copy:
    dest: /etc/multipath.conf
    content: |
      defaults {
          user_friendly_names yes
          find_multipaths yes
      }