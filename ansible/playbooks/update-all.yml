---
- name: Update and upgrade all remote hosts
  hosts: all
  become: true  # Run tasks with sudo privileges
  tasks:
    - name: Update apt package list
      apt:
        update_cache: yes

    - name: Upgrade all packages to the latest version
      apt:
        upgrade: yes
        autoremove: yes
        autoclean: yes


